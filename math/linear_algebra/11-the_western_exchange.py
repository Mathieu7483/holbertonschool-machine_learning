#!/usr/bin/env python3
""" file that contains the function np_transpose"""


def np_transpose(matrix):
    """function that transposes a matrix
    Returns:
        the transposed matrix
    """
    return matrix.T
# peut s'ecrire aussi return matrix.transpose()
